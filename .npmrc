# Windows Ctrl+C 卡终端修复:
# pnpm 在 Windows 默认经 cmd.exe 执行 package.json scripts, Ctrl+C 会触发
# "终止批处理操作吗(Y/N)?" 确认, 且在 pnpm 的 ANSI 输出下该提示常无法正常响应, 终端表现为卡死。
# shell-emulator=true 让 pnpm 用内置 sh 模拟器直接执行脚本(不派生 cmd), Ctrl+C 即刻终止
# nuxi 的单一 Node 进程; 同时 && 等 POSIX 语法在 dev:fresh 这类链式脚本中行为也与 macOS/Linux 一致。
shell-emulator=true
